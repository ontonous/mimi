# 0.1.7 Final Gate Evidence

> Status: 0.1.7 Wave-3 基建诚实收口证据快照（2026-08-19）。
> 终测结论见 `v0.37/quad-final-0.37.135.md`。
> 24h long soak is configured via `scripts/soak_nightly.sh` and remains the only
> externally time-gated item; it does not block 0.1.7 closeout.

## 1. Backend unification

| Gate | Result |
|---|---|
| `scripts/dispatch_stat.py check --zero` | fallback_rate = 0.0000 |
| Real-world | `cargo test --test real_world -- --test-threads=1` 31 passed |
| Real-world CLI | `cargo test --test real_world_cli -- --test-threads=1` passed |

## 2. Memory safety

| Gate | Result |
|---|---|
| Valgrind suite | `e2e_valgrind_` 18/18 passed |
| ASan nightly | lexer + parser fuzz (10) + property (44) passed |
| TSan nightly | runtime future (5) + FFI runtime (15) + actor concurrent (11) passed |
| Native soak smoke | 5s and 60s passed; RSS stable |
| 24h soak | `scripts/soak_nightly.sh` ready; actual 24h run pending |

## 3. Concurrency and high pressure

| Gate | Result |
|---|---|
| Flow event storm | 10,000,000 native events passed |
| Wire fuzzing | 10,000,000 decode iterations passed |
| Stress heavy | `make test-stress-heavy` 28 passed |
| Stress smoke | `make test-stress` 62 passed / 27 ignored |
| Dogfood | `make test-dogfood` passed |

## 4. Diagnostics and DX

| Gate | Result |
|---|---|
| LSP on 10k-line file | hover median ≈6.9ms; completion median ≈6.4ms |
| LSP regression gate | `e2e_perf_10k_line_completion_sub_10ms` passed |

## 5. Phase E final checks

| Gate | Result |
|---|---|
| quote removed | parser rejects; tests/docs migrated |
| protocol removed | parser/recovery/checker/codegen/interp clean; docs updated |
| effect/MTE dead-link | all `docs/` local Markdown links resolve |
| Wire CLI | `mimi wire encode\|decode\|validate-schema` added; roundtrip/corrupt/schema tests pass |
| .mimiabi CLI | `mimi abi core\|export\|validate\|hash\|diff\|check\|emit-c\|emit-rust\|emit-go\|emit-node\|emit-py\|emit-java\|emit-cpp` added; core ABI exports/validates clean |

## 6. Standard gates

| Gate | Result |
|---|---|
| `cargo test --lib` | 5421 passed / 7 ignored |
| `cargo fmt -- --check` | clean |
| `cargo clippy --all-targets -- -D warnings` | clean |

## 7. Closeout

| Item | Result |
|---|---|
| Quad-final | `v0.37/quad-final-0.37.135.md` lists §4 不宣称 |
| User-facing status | Wave-3 基建诚实收口已完成；不宣称内核愿景已闭合 / VM≡native / Flow 世代已实现 |
| `0.1.7` tag / crates | not cut (explicit Non-goal) |
