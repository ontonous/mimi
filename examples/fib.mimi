// Mimi v0.1 示例：斐波那契

func fib(n: i32) -> i32 {
    requires: n >= 0
    ensures: result >= 0

    if n <= 1 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

func main() -> i32 {
    let n = 10;
    println("fib(", n, ") =", fib(n));
    return fib(n);
}
