func make_text() -> string {
    "canonical"
}

func consume_text(text: string) -> i32 {
    ensures: result == 41
    drop(text);
    41
}

func main() -> i32 {
    let text = make_text();
    let cloned = text;
    drop(cloned);
    consume_text(text)
}
