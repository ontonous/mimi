// Homogeneous Result<T, T> remains outside the generic Result f64 island.
func unwrap<T>(value: Result<T, T>) -> T {
    value.unwrap()
}

func main() -> i64 {
    let value: Result<f64, f64> = Ok(41.5)
    let out = unwrap(value)
    42
}
