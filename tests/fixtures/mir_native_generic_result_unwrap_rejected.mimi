// Unsupported managed Result error shape remains fail-closed: only Copy i32
// or bool Err slots are admitted by the generic projection contract.
func unwrap<T>(value: Result<T, string>) -> T {
    value.unwrap()
}

func main() -> i32 {
    let value = unwrap(Ok("owned"))
    drop(value)
    41
}
