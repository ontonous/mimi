func unwrap_or<T>(value: Result<T, i32>, fallback: T) -> T {
    value.unwrap_or(fallback)
}

func main() -> i64 {
    let value: Result<string, i32> = Err(7)
    let selected = unwrap_or(value, "owned")
    drop(selected)
    9
}
