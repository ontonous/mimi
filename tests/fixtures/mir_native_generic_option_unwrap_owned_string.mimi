// Generic Option<T>.unwrap specialized to the canonical move-owned String ABI.
func unwrap<T>(value: Option<T>) -> T {
    value.unwrap()
}

func main() -> i32 {
    ensures: result == result
    let value: Option<string> = Some("owned")
    let out = unwrap(value)
    drop(out)
    41
}
