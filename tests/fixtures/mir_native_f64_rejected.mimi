// Canonical MIR accepts scalar f64 constants and finite-only Add/Subtract,
// but f64 multiplication remains outside the closed binary contract and must
// fail before LLVM declarations.
func main() -> f64 { 1.0 * 2.0 }
