type Point { x: i64, y: i64 }
flow Counter {
    state Zero { n: i32 }
    transition inc(Zero) -> Zero { return Zero { n: self.n + 1 } }
}
func main() -> i32 {
    let mut acc = 1;
    acc = acc + 1;
    let p = Point { x: 2, y: 3 };
    println(p.x);
    println(acc);
    0
}
