// Direct concrete Result<f64, i32> remains outside the promoted generic island.
func unwrap(value: Result<f64, i32>) -> f64 {
    value.unwrap()
}

func main() -> i64 {
    let value: Result<f64, i32> = Ok(41.5)
    let out = unwrap(value)
    42
}
