trait Desc {
    func describe() -> string
}

type Dog { name: string }

impl Desc for Dog {
    func describe() -> string {
        "Dog: " + self.name
    }
}

func main() -> i32 {
    let d = Dog { name: "Rex" }
    if d.describe() == "Dog: Rex" { 0 } else { 1 }
}
