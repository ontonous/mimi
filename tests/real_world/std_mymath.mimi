use std::mymath

func main() -> i32 {
    let a = abs(-5)
    let b = factorial(4)
    let c = is_prime(7)
    let d = sqrt_val(16.0)
    let diff = abs_float(d - 4.0)
    if a == 5 && b == 24 && c && diff < 0.001 { 0 } else { 1 }
}
