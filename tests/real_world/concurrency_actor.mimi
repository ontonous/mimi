actor Counter {
    mut count: i32 = 0;
    func increment() { self.count = self.count + 1 }
    func get() -> i32 { return self.count }
}

func main() -> i32 {
    let c = Counter.spawn()
    c.increment()
    c.increment()
    let v = c.get()
    if v == 2 { 0 } else { 1 }
}
